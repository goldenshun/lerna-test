{
  "name": "root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "bootstrap": "lerna bootstrap --hoist",
    "dev:packages": "lerna run dev --parallel",
    "publish": "PUBLISH_ENABLED=true lerna publish"
  },
  "devDependencies": {
    "@babel/core": "7.1.2",
    "@babel/preset-env": "7.1.0",
    "lerna": "^3.4.1"
  }
}
